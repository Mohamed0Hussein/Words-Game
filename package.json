{
  "name": "assesment_nagwa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "\"npm --prefix ./client run start\"",
    "server" : "\"npm --prefix ./server run dev\" ",
    "initialize" : " concurrently  \"npm --prefix ./client  install\" \"npm --prefix ./server install\" ",
    "project" : "concurrently   \" npm run server \"  \" npm run client\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
